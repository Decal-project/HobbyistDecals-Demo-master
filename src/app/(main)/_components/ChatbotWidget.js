'use client';

import React from 'react';
import styles from './ChatbotIcon.module.css'; // Create this CSS module if you want

const ChatbotWidget = () => {
  return (
    <div className={styles.chatbotWidgetContainer}>
      <p>Hey there how can I help you today!!!!</p>
      {/* You'll add your actual chatbot UI elements here later */}
    </div>
  );
};

export default ChatbotWidget;